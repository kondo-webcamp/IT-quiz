<div class="user-sign">
  <h2 class="admin-login-space">こちらは管理者側のログイン画面です</h2>
  <p>※管理者用のメールアドレスとパスワードが必要になります。</p>

  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="sign-form">
      <p class="sign-form-left"><%= f.label :'メールアドレス' %></p>
      <p class="sign-form-right"><%= f.email_field :email, autofocus: true, autocomplete: "email" %></p>
    </div>

    <div class="sign-form">
      <p class="sign-form-left"><%= f.label :'パスワード' %></p>
      <p class="sign-form-right"><%= f.password_field :password, autocomplete: "current-password" %></p>
    </div>

    <div class="admin-login-space">
      <%= f.submit "ログイン", class: 'next-button' %>
    </div>
  <% end %>

<%= render "admin/shared/links" %>
</div>
